# Example: NumPy

# * The python libraries are already available with different functions.
# We can use them to perform different tasks.

# Write a program to use numpy library to create an array and display the values.

import numpy as np

arr1 = np.array([2, 4, 6,8])
print(arr1)